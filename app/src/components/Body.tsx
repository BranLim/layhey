import { Container } from '@chakra-ui/react';
import { ActionMenu } from '@/components/ActionMenu';

export const Body = () => {
  return (
    <Container>
      <ActionMenu />
    </Container>
  );
};
